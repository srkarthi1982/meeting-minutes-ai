---
import "@ansiversa/components/styles/global.css";
import AvWebLayout from "./AvWebLayout.astro";

interface Props {
  title?: string;
  description?: string;
}

const props = Astro.props;
---

<AvWebLayout {...props}>
  <slot />
</AvWebLayout>
